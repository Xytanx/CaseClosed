<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to CaseClosed</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            background: linear-gradient(to top right, #7ac3ff, #b1fade);
            text-align: center;
            position: relative;
        }

        nav {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 10;
            background-color: rgba(240, 240, 240, 0.9);
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #ddd;
        }

        .content-box {
            background-color: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15);
            width: 90%; /* Use percentage for responsiveness */
            max-width: 450px; /* Limit maximum width */
            background: linear-gradient(to bottom, #e0f7fa, #e8f5e9);
        }

        h1 {
            color: #333;
            margin-bottom: 20px;
            font-size: 24px; /* Adjust font size for smaller screens */
        }

        p {
            color: #666;
            margin-bottom: 30px;
            font-size: 16px; /* Adjust font size for smaller screens */
        }

        .button-container {
            display: flex;
            flex-direction: column; /* Stack buttons on smaller screens */
            align-items: center;
            gap: 20px;
        }

        button {
            padding: 15px 30px;
            font-size: 16px;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        button.client {
            background: linear-gradient(to bottom, #0587f1, #00abee);
            color: rgb(255, 255, 255);
        }

        button.provider {
            background: linear-gradient(to bottom, #90ee90, #228b22);
            color: white;
        }

        /* Media queries for responsiveness */
        @media (min-width: 600px) {
            .button-container {
                flex-direction: row; /* Display buttons in a row on larger screens */
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="content-box">
        <h1>Welcome to CaseClosed</h1>
        <p>Please select your user type to proceed.</p>
        <div class="button-container">
            <form action="/client" method="GET">
                <button type="submit" class="client">Client</button>
            </form>
            <form action="/service_provider" method="GET">
                <button type="submit" class="provider">Service Provider</button>
            </form>
        </div>
    </div>
</body>
</html>